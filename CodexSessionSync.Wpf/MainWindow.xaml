<Window x:Class="CodexSessionSync.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Codex 会话同步工具"
        Width="1180"
        Height="900"
        MinWidth="1040"
        MinHeight="760"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI, Microsoft YaHei UI"
        Background="{DynamicResource AppBackgroundBrush}">
    <Window.Resources>
        <SolidColorBrush x:Key="AppBackgroundBrush" Color="#0F1117"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#1B1F2A"/>
        <SolidColorBrush x:Key="InputBackgroundBrush" Color="#272C36"/>
        <SolidColorBrush x:Key="OutputBackgroundBrush" Color="#101827"/>
        <SolidColorBrush x:Key="BorderBrushSoft" Color="#343B4A"/>
        <SolidColorBrush x:Key="BorderBrushStrong" Color="#465064"/>
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#B9C0CC"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#9AA3B2"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#4CC2FF"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#D92D20"/>
        <SolidColorBrush x:Key="SecondaryButtonBrush" Color="#2A2F3A"/>
        <SolidColorBrush x:Key="ActiveCardBackgroundBrush" Color="#253044"/>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        </Style>

        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        </Style>

        <Style x:Key="HelpTextStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,8,0,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="FluentTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="MaxWidth" Value="390"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="Chrome" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OutputTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontFamily" Value="Consolas, Cascadia Mono"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CaretBrush" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>

        <Style x:Key="ModeRadioStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="34"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid Grid.Column="0" Width="24" Height="24" VerticalAlignment="Top" Margin="0,2,0,0">
                                <Ellipse x:Name="OuterRing"
                                         Stroke="{DynamicResource MutedTextBrush}"
                                         StrokeThickness="1.6"
                                         Fill="Transparent"/>
                                <Ellipse x:Name="InnerDot"
                                         Width="12"
                                         Height="12"
                                         Fill="Transparent"
                                         HorizontalAlignment="Center"
                                         VerticalAlignment="Center"/>
                            </Grid>
                            <ContentPresenter Grid.Column="1"
                                              Content="{TemplateBinding Content}"
                                              VerticalAlignment="Top"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="OuterRing" Property="Stroke" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="OuterRing" Property="StrokeThickness" Value="4"/>
                                <Setter TargetName="InnerDot" Property="Fill" Value="{DynamicResource AppBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FluentButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Padding" Value="18,0"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource SecondaryButtonBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.78"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.45"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButtonStyle" TargetType="Button" BasedOn="{StaticResource FluentButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="#07111D"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource FluentButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource DangerBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DangerBrush}"/>
        </Style>
    </Window.Resources>

    <Grid x:Name="RootGrid" Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,22">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Codex 会话同步工具"
                           FontSize="30"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           Margin="0,0,0,4"/>
                <TextBlock Text="默认预览，不会写入；勾选确认后点击执行写入才会修改文件和 SQLite。"
                           FontSize="16"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           TextWrapping="Wrap"/>
            </StackPanel>

            <Button x:Name="ThemeToggleBtn"
                    Grid.Column="1"
                    Content="☀"
                    Width="44"
                    Height="44"
                    MinWidth="44"
                    Margin="0,0,16,0"
                    Padding="0"
                    FontSize="24"
                    Style="{StaticResource FluentButtonStyle}"
                    ToolTip="切换浅色/深色"
                    Click="OnThemeToggleClick"/>

            <Border Grid.Column="2"
                    Background="{DynamicResource SecondaryButtonBrush}"
                    BorderBrush="{DynamicResource BorderBrushStrong}"
                    BorderThickness="1"
                    CornerRadius="4"
                    MinWidth="150"
                    Padding="18,10"
                    VerticalAlignment="Center">
                <TextBlock x:Name="StatusLabel"
                           Text="准备就绪"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           FontSize="16"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"/>
            </Border>
        </Grid>

        <Border Grid.Row="1"
                Background="{DynamicResource CardBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="20"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" MinWidth="0">
                    <TextBlock Text="Codex Home" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox x:Name="CodexHomeBox" Style="{StaticResource FluentTextBoxStyle}"/>
                    <TextBlock Text="默认读取 CODEX_HOME 或当前用户的 .codex。"
                               Style="{StaticResource HelpTextStyle}"/>
                </StackPanel>

                <StackPanel Grid.Column="2" MinWidth="0">
                    <TextBlock Text="备份目录" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox x:Name="BackupDirBox" Style="{StaticResource FluentTextBoxStyle}"/>
                    <TextBlock Text="执行写入时建议填写，例如桌面上的 codex-backup。"
                               Style="{StaticResource HelpTextStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Background="{DynamicResource CardBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="20"
                Margin="0,0,0,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="同步模式"
                           Style="{StaticResource SectionTitleStyle}"
                           Margin="0,0,0,14"/>

                <Grid Grid.Row="1" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="14"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="14"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border x:Name="ModeMutualCard"
                            Grid.Column="0"
                            MinWidth="0"
                            MaxWidth="340"
                            Background="{DynamicResource SecondaryButtonBrush}"
                            BorderBrush="{DynamicResource BorderBrushStrong}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="14"
                            Cursor="Hand"
                            MouseLeftButtonDown="OnModeCardClick">
                        <RadioButton x:Name="ModeMutual"
                                     GroupName="Mode"
                                     IsChecked="True"
                                     Style="{StaticResource ModeRadioStyle}"
                                     Checked="OnModeChanged">
                            <StackPanel>
                                <TextBlock Text="全供应商互同步"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBlock Text="配置中的每个 provider 最终看到同一批会话。"
                                           Margin="0,4,0,0"
                                           FontSize="13"
                                           Foreground="{DynamicResource SecondaryTextBrush}"
                                           TextWrapping="Wrap"
                                           MaxWidth="250"/>
                            </StackPanel>
                        </RadioButton>
                    </Border>

                    <Border x:Name="ModeOpenAiCard"
                            Grid.Column="2"
                            MinWidth="0"
                            MaxWidth="340"
                            Background="{DynamicResource SecondaryButtonBrush}"
                            BorderBrush="{DynamicResource BorderBrushStrong}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="14"
                            Cursor="Hand"
                            MouseLeftButtonDown="OnModeCardClick">
                        <RadioButton x:Name="ModeOpenAi"
                                     GroupName="Mode"
                                     Style="{StaticResource ModeRadioStyle}"
                                     Checked="OnModeChanged">
                            <StackPanel>
                                <TextBlock Text="OpenAI 同步到全部"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBlock Text="以 source provider 为源，给其它 provider 创建镜像。"
                                           Margin="0,4,0,0"
                                           FontSize="13"
                                           Foreground="{DynamicResource SecondaryTextBrush}"
                                           TextWrapping="Wrap"
                                           MaxWidth="250"/>
                            </StackPanel>
                        </RadioButton>
                    </Border>

                    <Border x:Name="ModeMigrateCard"
                            Grid.Column="4"
                            MinWidth="0"
                            MaxWidth="340"
                            Background="{DynamicResource SecondaryButtonBrush}"
                            BorderBrush="{DynamicResource BorderBrushStrong}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="14"
                            Cursor="Hand"
                            MouseLeftButtonDown="OnModeCardClick">
                        <RadioButton x:Name="ModeMigrate"
                                     GroupName="Mode"
                                     Style="{StaticResource ModeRadioStyle}"
                                     Checked="OnModeChanged">
                            <StackPanel>
                                <TextBlock Text="单目标迁移"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBlock Text="把非保留 provider 的历史会话迁移到目标 provider。"
                                           Margin="0,4,0,0"
                                           FontSize="13"
                                           Foreground="{DynamicResource SecondaryTextBrush}"
                                           TextWrapping="Wrap"
                                           MaxWidth="250"/>
                            </StackPanel>
                        </RadioButton>
                    </Border>
                </Grid>

                <Grid Grid.Row="2" Margin="0,0,0,18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="18"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" MinWidth="0">
                        <TextBlock Text="源 provider" Style="{StaticResource FieldLabelStyle}"/>
                        <TextBox x:Name="SourceProviderBox" Text="openai" Style="{StaticResource FluentTextBoxStyle}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" MinWidth="0">
                        <TextBlock Text="目标 provider" Style="{StaticResource FieldLabelStyle}"/>
                        <TextBox x:Name="TargetProviderBox" Text="openai" Style="{StaticResource FluentTextBoxStyle}"/>
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="3" Orientation="Horizontal">
                    <Button x:Name="PreviewBtn"
                            Content="预览"
                            Width="100"
                            Style="{StaticResource AccentButtonStyle}"
                            Click="OnPreviewClick"
                            Margin="0,0,10,0"/>
                    <Button x:Name="ApplyBtn"
                            Content="执行写入"
                            Width="112"
                            Style="{StaticResource DangerButtonStyle}"
                            Click="OnApplyClick"
                            Margin="0,0,10,0"/>
                    <Button x:Name="DefaultsBtn"
                            Content="恢复默认路径"
                            MinWidth="128"
                            Style="{StaticResource FluentButtonStyle}"
                            Click="OnResetDefaults"
                            Margin="0,0,18,0"/>
                    <CheckBox x:Name="ConfirmCheck"
                              VerticalAlignment="Center"
                              Foreground="{DynamicResource PrimaryTextBrush}"
                              FontSize="15"
                              Content="我已备份或确认可以写入"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="3"
                Background="{DynamicResource OutputBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="20"
                MinHeight="180">
            <TextBox x:Name="OutputBox"
                     Style="{StaticResource OutputTextBoxStyle}"
                     Text="等待运行。"/>
        </Border>
    </Grid>
</Window>
